name: scp files
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Copy files via SSH
        uses: appleboy/scp-action@v1
        with:
          host: 85.10.192.25
          username: anantakandel
          password: ${{ secrets.PASSWORD }}
          port: 22
          source: "CICD-on-Cpanel-account-using-scp"
          target: /home/anantakandel/public_html/CICD-on-Cpanel-account-using-scp
